spring.jpa.hibernate.ddl-auto=validate
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false

# No fallback is intentionally provided in production.
app.jwt.secret=${LOGITRACK_JWT_SECRET}
